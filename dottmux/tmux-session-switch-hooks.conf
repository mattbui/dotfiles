# Move the changed client's current session to the front of the server-wide MRU.
if-shell "'${HOME}/.config/tmux/scripts/tmux-min-version.sh' 3.2" \
  "set-hook -g client-session-changed { run-shell \"\\\"${HOME}/.config/tmux/scripts/switch-session-mru.sh\\\" record #{q:hook_client}\" }"
